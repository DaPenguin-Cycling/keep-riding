source "https://rubygems.org"

# Pinned to the same gem GitHub Pages runs for a legacy build, so a local
# `bundle exec jekyll serve` matches production exactly.
gem "github-pages", group: :jekyll_plugins
